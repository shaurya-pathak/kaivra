bump: major
Changed: relative motion translations replace pixel offsets for move and move-to animations; the unused absolute layout mode is removed from the DSL surface; legacy offset_x/offset_y fields are rejected with a clear error.
