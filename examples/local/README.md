Local-only example drafts live here.

Use this directory for:
- alternate versions of curated explainers/reference files
- scratch examples you want to render locally without committing
- temporary MCP output you want to compare against the tracked examples

If an example becomes part of the curated repo set, move it into the appropriate
tracked folder under `examples/`.
